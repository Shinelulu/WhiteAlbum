// initial state
const state = {
    earthReady: false
}

// getters
const getters = {}

// actions
const actions = {}

// mutations
const mutations = {
    setEarthReady(state) {
        state.earthReady = true
    }
}

export default {
    state,
    getters,
    actions,
    mutations
}